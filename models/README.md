# Models Directory

Auto-generated .pkl files appear here after first run.
These are excluded from git — they regenerate automatically on startup.

Files created:
  rf_model.pkl
  xgb_model.pkl
  lr_model.pkl
  scaler.pkl
  encoders.pkl
  stats.json
